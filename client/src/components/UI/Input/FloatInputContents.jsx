import React from 'react';
import './FloatInput.css';

const FloatInputContents = ({ children }) => {
    return <div className="floating-label-contents">{children}</div>;
};

export default FloatInputContents;
